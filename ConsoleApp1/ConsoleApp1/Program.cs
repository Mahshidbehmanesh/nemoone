using System;

namespace ConsoleApp1
{
    class calculator 
    {
        private double x2;
        private double y2;
        private double x;
        private double y;
        public calculator() { }

        private double calculator(double x, double y, double x2, double y2)

        {
            y = y;
            X = x;
           X2 = x2;
            Y = y2;
        }
        class program
        {
            public double result()
            {
              double  d = Math.Sqrt(Math.Pow((x2 - x), 2 + Math.Pow(y2 - y, 2));
            }
            static void Main(string[] args)
            {

            }
        }
    }
}